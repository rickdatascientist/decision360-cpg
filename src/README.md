# Implementation

Implementation for Decision360 CPG lives here. The first accepted code must trace to an approved workflow, acceptance test, evidence source, and product decision.

Do not add an agent layer before the deterministic decision path and failure behavior are testable.
